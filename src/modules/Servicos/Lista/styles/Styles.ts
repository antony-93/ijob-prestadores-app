import { StyleSheet } from "react-native";

export const listaServicosStyles = StyleSheet.create({
    title: {
        fontSize: 40,
        fontWeight: 'bold',
    },

    headerServices: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        width: '100%',
        marginTop: 10
    }
})